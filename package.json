{
  "name": "dragonfly-cli",
  "version": "1.0.0",
  "description": "Huawei DevUI前端脚手架",
  "bin": {
    "dgf": "bin/dragonfly.js"
  },
  "scripts": {
    "start": "tsc -p . -w",
    "prebuild": "npm run copyfile",
    "build": "tsc -p .",
    "copyfile": "rm -rf dist && mkdir dist && cp -r ./bin ./dist/",
    "eslint": "npx eslit . --ext ts --ignore-path .gitignore",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.1",
    "chalk": "^5.2.0",
    "commander": "^9.4.1",
    "inquirer": "^9.1.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "eslint": "^8.30.0",
    "prettier": "^2.8.1",
    "typescript": "^4.9.4"
  }
}
