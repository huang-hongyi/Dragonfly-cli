import { DragonfyInitOptions } from '../models/cli';

export async function dragonflyInit(dirName: string, options: DragonfyInitOptions) {
  console.log('????');
  console.log('初始化', dirName, options);
}